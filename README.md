# Parallel Works Homebrew Tap

This is a custom Homebrew tap for official Parallel Works software.

## Setup

You can add the custom tap on MacOS by running:

```shell
brew tap parallelworks/brew
```
